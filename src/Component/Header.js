import '../App.css';
import SearchBar from './SearchBar';

function Header() {
  return (
<div class="header">
  <h1>Precision Scoop</h1>
  <p>Scoop up result</p>
  <SearchBar/>
</div>

  );
}

export default Header;
